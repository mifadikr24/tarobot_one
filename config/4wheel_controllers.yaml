controller_manager:
  ros__parameters:
    update_rate: 50
    # use_sim_time: true

    diff_cont:
      type: diff_drive_controller/DiffDriveController

    joint_broad:
      type: joint_state_broadcaster/JointStateBroadcaster

diff_cont:
  ros__parameters:

    publish_rate: 50.0

    base_frame_id: base_footprint
    odom_frame_id: odom
    enable_odom_tf: false
    # open_loop: true
    pose_covariance_diagonal: [0.0025, 0.0025, 1000000.0, 1000000.0, 1000000.0, 0.02]
    twist_covariance_diagonal: [0.005, 0.005, 1000000.0, 1000000.0, 1000000.0, 0.03]

    left_wheel_names: ['fl_wheel_joint', 'rl_wheel_joint']
    right_wheel_names: ['fr_wheel_joint', 'rr_wheel_joint']
    wheel_separation: 0.327 #0.365 phisycally 0.205
    wheel_radius: 0.0508    

    # wheels_per_side: 1
    # wheel_separation_multiplier: 1.0
    # left_wheel_radius_multiplier: 1.0
    # right_wheel_radius_multiplier: 1.0

    # cmd_vel_timeout: x
    # publish_limited_velocity: x
    # velocity_rolling_window_size: 5

    # use_stemped_vel: false

    # linear.x.has_velocity_limits: true
    # linear.x.has_acceleration_limits: true
    # linear.x.has_jerk_limits: false
    # linear.x.max_velocity: 0.30
    # linear.x.min_velocity: -0.30
    # linear.x.max_acceleration: 1.0
    # linear.x.min_acceleration: -1.0
    # linear.x.max_jerk: NAN
    # linear.x.min_jerk: NAN

    # angular.z.has_velocity_limits: true
    # angular.z.has_acceleration_limits: true
    # angular.z.has_jerk_limits: false
    # angular.z.max_velocity: 4.0
    # angular.z.min_velocity: -4.0
    # angular.z.max_acceleration: 4.0
    # angular.z.min_acceleration: -4.0
    # angular.z.max_jerk: NAN
    # angular.z.min_jerk: NAN

# joint_broad:
#   ros__parameters: